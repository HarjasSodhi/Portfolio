export type exp = {
    position: string,
    company: string,
    link: string,
    startDate: string,
    endDate: string,
    bullets: string[]
}

export type proj = {
    link?: string,
    github?: string,
    title: string,
    description: string,
    stack: string
}